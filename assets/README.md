# assets/

Static, non-code files the pipeline references at runtime: sample sheets,
reference schema (e.g. JSON schema for --input validation), small
reference data, email/report templates, etc.

Not for large reference genomes/datasets - those should be pulled in via
params pointing at external storage, not committed here.
